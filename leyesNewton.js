$('#limpiar').click(function() {})

const calcularTension = (peso, angulo) => {
    let tension = (peso * (Math.cos(toRadians(angulo)))).toFixed(3)
    return tension
}
$(calcular).click(function() {

})